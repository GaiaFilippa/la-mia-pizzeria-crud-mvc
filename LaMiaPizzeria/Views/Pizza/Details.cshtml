@model Pizza

<div class="container">
    <div class="row">
        <div class="col-12">
            <div class="d-flex align-items-center">
                <h1 class="mt-3 mb-3">Dettagli Articolo</h1>
            </div>
            <div class="post-single-image-container">
                <img src="@Model.Image" alt="Immagine @Model.Name" width="200">
            </div>
            <p>
                <strong>Nome:</strong>
                @Model.Name
            </p>
            <p>
                <strong>Descrizione:</strong>
                @Model.Description
            </p>
        </div>
    </div>
</div>